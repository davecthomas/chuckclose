---
timestamp: "2026-04-03T21:06:51Z"
author: "davidcthomas"
branch: "main"
thread_id: "db1e906f-a9dc-4a61-823f-471349f95238"
turn_id: "turn_0230dd1d47"
decision_candidate: true
ai_generated: true
ai_model: "gpt-5.4"
ai_tool: "codex"
ai_surface: "codex-cli"
ai_executor: "local-agent"
related_adrs:
files_touched:
  - "env_template.txt"
  - "pyproject.toml"
verification:
  - "### Key Error: Wrong Directory"
  - "The Write calls failed (files need to be read first), so no actual damage was done. User clarified: \"I told you clearly where the files are: ~/.agent/skills\""
  - "Message: \"main: Improve mosaic codebase quality and fix test stubs\""
  - "Initial examples used `python -m mosaic` \u2192 got \"No module named mosaic\" error"
  - "### Initial Error: Wrong PyPI Version"
  - "**Editing wrong directory:** Started writing skills to `/Users/davidthomas/code/upside/upside-ai-skillz/skills/` instead of `~/.agent/skills/`. User: \"why the hell are you editing files in that folder!!!!\". Write calls failed (files need to be read first) so no damage. Fixed by working only in `~/.agent/skills/`."
  - "**Gemini auth failure:** Root cause was `get_setting()` bug in ai-api-unified not reading `GOOGLE_AUTH_METHOD` from `.env`. v2.5.0 (local library) already contains the fix: checks `self.model_extra` before falling back to `os.environ.get()`."
  - "\"For some reason, it's not reading the env file, perhaps. I don't know why I'm seeing this error because the environment variable is correctly set. [auth error]\""
  - "\"to test this out, I'd like to use the ai api library located at ../../aiapi/ai_api_unified rather than the pypi one. Can you fix this?\""
  - "**Add `COMPLETIONS_MODEL_NAME=gemini-3.0-flash` to `.env`** \u2014 still missing, causes `gpt-4o-mini` fallback warning"
  - "**Test end-to-end storyboard pipeline with Gemini** \u2014 user said \"ok I'll get the library fixed and then we'll resume work later\" indicating this is the main pending task once the ai_api_unified `get_setting` fix is confirmed"
---

## Why

- <analysis>

## Repo changes

- Updated env_template.txt
- Updated pyproject.toml

## Evidence

- ### Key Error: Wrong Directory
- The Write calls failed (files need to be read first), so no actual damage was done. User clarified: "I told you clearly where the files are: ~/.agent/skills"
- Message: "main: Improve mosaic codebase quality and fix test stubs"
- Initial examples used `python -m mosaic` → got "No module named mosaic" error
- ### Initial Error: Wrong PyPI Version
- **Editing wrong directory:** Started writing skills to `/Users/davidthomas/code/upside/upside-ai-skillz/skills/` instead of `~/.agent/skills/`. User: "why the hell are you editing files in that folder!!!!". Write calls failed (files need to be read first) so no damage. Fixed by working only in `~/.agent/skills/`.
- **Gemini auth failure:** Root cause was `get_setting()` bug in ai-api-unified not reading `GOOGLE_AUTH_METHOD` from `.env`. v2.5.0 (local library) already contains the fix: checks `self.model_extra` before falling back to `os.environ.get()`.
- "For some reason, it's not reading the env file, perhaps. I don't know why I'm seeing this error because the environment variable is correctly set. [auth error]"
- "to test this out, I'd like to use the ai api library located at ../../aiapi/ai_api_unified rather than the pypi one. Can you fix this?"
- **Add `COMPLETIONS_MODEL_NAME=gemini-3.0-flash` to `.env`** — still missing, causes `gpt-4o-mini` fallback warning
- **Test end-to-end storyboard pipeline with Gemini** — user said "ok I'll get the library fixed and then we'll resume work later" indicating this is the main pending task once the ai_api_unified `get_setting` fix is confirmed

## Next

- Review the generated shard and summary, then explicitly commit and push them with the related code changes if ready.
