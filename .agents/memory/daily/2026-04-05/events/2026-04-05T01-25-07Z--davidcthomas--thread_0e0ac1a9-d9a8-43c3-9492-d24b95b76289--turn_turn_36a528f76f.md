---
timestamp: "2026-04-05T01:25:07Z"
author: "davidcthomas"
branch: "feature/storyboard-video-generation"
thread_id: "0e0ac1a9-d9a8-43c3-9492-d24b95b76289"
turn_id: "turn_36a528f76f"
decision_candidate: false
ai_generated: true
ai_model: "gpt-5.4"
ai_tool: "gemini"
ai_surface: "gemini-cli"
ai_executor: "local-agent"
related_adrs:
files_touched:
  - "doc/mosaic_design_specification.md"
verification:
  - "git diff:  1 file changed, 248 insertions(+), 1 deletion(-); ## 23. AI Video Generation Storyboard Pipeline; ### 23.1 Objective; **Add AI video generation** (e.g., Google Veo) as a primary source for multi-frame storyboard mosaics. The system supports two distinct storyboard paths:"
---

## Why

- 1. Great point. fix the design so the system supports either image or video for storyboard mosaics. 2. Fine also make sure that the installer does what is needed so that never happens. This means we need some kind of installer for this or will poetry do the trick? 3. Just update the design to get the nearest keyframe without making the user interface worry about it. 4. Nah

## Repo changes

- Updated doc/mosaic_design_specification.md

## Evidence

- git diff:  1 file changed, 248 insertions(+), 1 deletion(-); ## 23. AI Video Generation Storyboard Pipeline; ### 23.1 Objective; **Add AI video generation** (e.g., Google Veo) as a primary source for multi-frame storyboard mosaics. The system supports two distinct storyboard paths:

## Next

- 2.  **Dependency Hardening:** I will add a requirement for a `setup.sh` or a pre-flight check in the CLI to ensure `ffmpeg` is present. While `poetry` handles Python packages, it cannot natively install system binaries like `ffmpeg`, so the design will now include an explicit "Environment Validation" step.
- 2.  **Dependency Hardening:** I will add a requirement for a `setup.sh` or a pre-flight check in the CLI to ensure `ffmpeg` is present. While `poetry` handles Python packages, it cannot natively  install system binaries like `ffmpeg`, so the design will now include an explicit "Environment Validation" step.
